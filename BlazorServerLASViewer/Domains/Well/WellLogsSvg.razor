@using BlazorServerLASViewer.Domains.Well

@for (var i = 0; i < @Well.Data.Logs.Length; i++)
{
        @for (var j = 0; j < @Well.Data.Logs[i].Length; j++)
        {
            <LogDatumSvgTrace datum="@Well.Data.Logs[i][j]"> </LogDatumSvgTrace>
        }
}

@code {
    [Parameter]
    public Well Well { get; set; }
}
