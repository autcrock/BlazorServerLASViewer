@page "/loadlogasciilasfile"
@inject MainViewModel MainViewModel

@using BlazorServerLASViewer.Domains.Well;

<h1>Log ASCII (LAS) file loader</h1>

<InputFile OnChange="LoadLogASCII" />

@if (File == null)
{
    <p> No file selected</p>
}
else
{

    if (MainViewModel.Well == null || MainViewModel.Well.DoubleLogs == null || MainViewModel.Well.DoubleLogs.Headers == null || MainViewModel.Well.DoubleLogs.Logs == null)
    {
        <p>Loading log ASCII format file : @File.Name (@File.Size bytes)</p>
    }
    else
    {
        <style>
            th, td {
                padding: 5px;
            }
        </style>

        <hr size="2" noshade>

        <WellSvg> </WellSvg>

        <hr size="2" noshade>

        <table>
            <thead>
                <tr>
                    <th> File Name </th>
                    <th> File Size </th>
                    <th> Number of Log Headers </th>
                    <th> Number of Logs </th>
                    <th> Last Modified </th>
                </tr>
            </thead>

            <tbody>
                <tr>
                    <td>@File.Name </td>
                    <td>@File.Size</td>
                    <td>@MainViewModel.Well.DoubleLogs.Headers.Length</td>
                    <td>@MainViewModel.Well.DoubleLogs.Logs.Length</td>
                    <td>@File.LastModified.ToShortDateString()</td>
                </tr>
            </tbody>
        </table>
        <WellTable well="@MainViewModel.Well"></WellTable>

        @code
        {
            private IFileListEntry File;

            async Task LoadLogASCII(IFileListEntry[] files)
            {
                File = files.FirstOrDefault();
                MainViewModel.Well = await Well.GetWellFromFileListEntryAsync(File);
                MainViewModel.Logs = Well.LogDataToLogs<double>(MainViewModel.Well.DoubleLogs);
            }

        }
    }
}